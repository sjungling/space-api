type Query {
    missions: [Mission]
}

type Mission {
  id: ID!
  mission: String!
  launchDate: String!
  astronauts: [Astronaut]
}

type Astronaut {
  id: ID!
  firstName: String!
  lastName: String!
}
